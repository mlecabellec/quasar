# Handover Report: ASAN Stabilization & Build Environment (2026-04-23)

**Date:** 2026-04-23  
**Status:** 🔄 **In Progress (Build Phase)**  
**Target:** Stabilization of AddressSanitizer (ASAN) as a Valgrind replacement.

## 1. Executive Summary
The project build environment has been enhanced to support global AddressSanitizer (ASAN) and ThreadSanitizer (TSAN) instrumentation. We have successfully verified ASAN's capability to detect memory corruption in the local environment and resolved several environmental conflicts (false positives in system headers and shared runtime library ordering). The project is currently in the middle of a full rebuild with ASAN enabled.

---

## 2. Completed Actions

### 🛡️ Build System Hardening
- **Global Options**: Added `ENABLE_ASAN` and `ENABLE_TSAN` options to `cmake-projects/CMakeLists.txt`.
- **Warning Suppression**: Forced `-Wno-maybe-uninitialized` when ASAN is active to bypass false positives in `std::regex` and other system headers that were breaking the build due to `-Werror`.
- **ASAN Verification**: Confirmed ASAN functionality via a targeted heap-buffer-overflow test case.

### 🐧 Environment Stabilization
- **Runtime Ordering**: Identified that the shared ASAN runtime requires `LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libasan.so.8` for test discovery (`gtest_discover_tests`) and execution.
- **LSAN/Ptrace Conflict**: Resolved a fatal LeakSanitizer crash during `make` (caused by environment restrictions on ptrace/task reading) by setting `ASAN_OPTIONS=detect_leaks=0:verify_asan_linkage=0`.

---

## 3. Current Status & Discrepancies
- **Build**: Rebuild is approximately 60% complete. `coretypes_test` and `quasar_named` are successfully instrumented.
- **Blocked**: `qlsh` test failures are pending investigation until the ASAN build completes.
- **Sanitizer vs. Valgrind**: ASAN is now the primary memory validation tool for this environment due to `mmap` errors encountered with Valgrind.

---

## 4. Immediate Next Steps
1. **Complete Build**: Run `cd build && export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libasan.so.8 && export ASAN_OPTIONS=detect_leaks=0:verify_asan_linkage=0 && make -j$(nproc)`.
2. **Run All Tests**: Execute `ctest` with the same `LD_PRELOAD` and `ASAN_OPTIONS`.
3. **Investigate `qlsh`**: Analyze the output of failing shell tests (suspected TUI/IO echoing issue).
4. **Resume Web UI**: Once core stability is verified, proceed with `webui` implementation.

---
*Report generated by Quasar Engineering Agent.*
